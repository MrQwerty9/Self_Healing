<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sstudio.selfhealing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adapter.DecisionRationalActivity"></activity>
        <activity android:name=".HealingDecisionActivity" />
        <activity android:name=".DecisionActivity" />
        <activity android:name=".HealingBehaviorActivity" />
        <activity android:name=".BehaviorActivity" />
        <activity android:name=".HealingEmotionActivity" />
        <activity android:name=".EmotionActivity" />
        <activity android:name=".HealingLocusActivity" />
        <activity android:name=".LocusActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>